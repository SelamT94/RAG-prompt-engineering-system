def generate_prompts(description, scenarios):
    # Implement sophisticated algorithms to generate prompts
    prompts = []
    for scenario in scenarios:
        prompt = f"{description} - Scenario: {scenario}"
        prompts.append(prompt)
    return prompts
